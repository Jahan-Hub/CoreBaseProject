using Microsoft.AspNetCore.Identity;

namespace BlazorApp1.Infrastructure.Models
{
    public class ApplicationUser : IdentityUser
    {
        // Extend IdentityUser with application-specific claims/properties later if needed
    }
}
